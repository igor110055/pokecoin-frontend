import styled from 'styled-components';

export const Header = styled.header`
    display: flex;
    width: 100%;
    height: 80px;
    background: var(--poke-blue);
    position: relative;
    z-index: 99;
`